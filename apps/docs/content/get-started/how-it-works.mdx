---
title: How a trade works
description: From a plain-English ask to a signed trade in your wallet — how DeepBookie prices it, builds it, and lets you sign.
---

# How a trade works

You ask in words. DeepBookie prices it, builds the transaction, and shows you
the receipt. You sign — and a trade is never live until **your** wallet signs
it. Here is the full loop, from question to confirmation.

<Steps
  items={[
    { title: 'You ask in plain language', body: '“Bet $50 that BTC is above $70k Friday.” Or “Swap 1 SUI for dUSDC.” No order forms, no strike pickers. DeepBookie turns your words into the right trade.' },
    { title: 'DeepBookie prices it', body: 'It reads the live market — odds for Predict, the order book for Spot — and computes the exact cost and payout. This is a free read; nothing is committed yet.' },
    { title: 'DeepBookie builds the transaction', body: 'You get an unsigned Sui transaction with a plain receipt: the amount, the price, what you stand to win. Review it before anything moves.' },
    { title: 'You sign in your wallet', body: 'Approve in your browser wallet (web), or with a local key on your machine (CLI/MCP). Signing is the only step that touches the chain. The receipt updates the moment it lands.' },
  ]}
/>

## See it live

This is the exact widget the agent renders in chat. Ask for odds, and you get the
probability curve across strikes:

<PromptDemo prompt="What are the odds BTC closes above $70k Friday?" tool="get_odds" label="OddsCurveCard">
  <OddsCurve />
</PromptDemo>

## Reading the receipt

Every receipt has a colored bar across the top. The color tells you the trade's
state at a glance:

- **Ink** — *proposed*. Priced and ready, not signed yet.
- **Green** — *signed*. The transaction landed on-chain.
- **Dashed** — *the trade did not happen, and nothing left your wallet.* You see
  this when:
  1. You declined the transaction in your wallet, or
  2. It failed on-chain (for example, not enough dUSDC to cover the bet), or
  3. You asked for a new bet before signing the old one, so the old proposal was
     replaced.

A dashed receipt is safe to ignore. Just ask again, or sign the latest one. Here
is the receipt in all six states, from loading to signed:

<SignReceiptStates />

<Callout type="note">
  Steps 1–2 are free reads. Only step 4 — your signature — writes to the chain.
</Callout>

## Why it works this way

The price is not a guess. DeepBookie computes it from a live volatility model —
see [Pricing](/concepts/pricing) if you want the math.

And DeepBookie never holds your key. It builds the transaction; you sign it
yourself, in your wallet or on your machine — never in the cloud. That split is
the whole design. Read [Sign at the edge](/concepts/sign-at-edge) for how it
keeps your funds yours.
