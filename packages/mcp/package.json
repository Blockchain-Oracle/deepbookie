{
  "name": "@deepbookie/mcp",
  "version": "0.0.1",
  "description": "DeepBookie MCP server (stdio) — trade DeepBook Predict from any MCP client; signs with a local key.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "deepbookie-mcp": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@deepbookie/core": "workspace:*",
    "@deepbookie/node": "workspace:*",
    "@deepbookie/predict-client": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@mysten/sui": "^2.19.0"
  }
}
